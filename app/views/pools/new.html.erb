<h1 class="text-center">Add your pool</h1>
<div class="container">
  <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
      <div class="card text-center" id="new-pool">
        <%= simple_form_for [@pool] do |f| %>
          <%= f.error_notification %>
          <div class="form-group">
            <h2 class="text-left"><i class="fa fa-quote-left"></i> Give a sexy name to your pool</h2>
            <%= f.input :title, label: false, placeholder: 'My big poooool' %>
          </div>
          <div class="form-group">
            <h2 class="text-left"><i class="fa fa-map-marker"></i> Address</h2>
            <%= f.input :address, label: false, placeholder: 'Marcel Pagnol Street, 32'  %>
            <%= f.input :city, label: false, collection: %w(Cannes Nice Palavas Valence Avignon Santa-Barbara Miami Monaco Yakutsk), prompt: 'Select your city' %>
          </div>
          <div class="form-group">
            <h2 class="text-left"><i class="fa fa-money"></i> Price and capacity</h2>
            <%= f.input :price, label: false, placeholder: '$ 10 - Enter a price per hour' %>
            <%= f.input :capacity, label: false, placeholder:'6 persons, the more, the better' %>
          </div>
          <div class="form-group">
            <h2 class="text-left"><i class="fa fa-glass"></i> A brief description</h2>
            <%= f.input :content, label: false, placeholder: 'Great pool! Sun is shining till late in afternoon, great to chill out by the pool with chicks!' %>
          </div>
          <%= f.button :submit, class: 'btn-danger' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
